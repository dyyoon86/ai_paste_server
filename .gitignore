# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# electron-builder output
/dist-app

# 프로젝트 전용 node 바이너리 (커밋 안 함; CI 는 actions/setup-node 사용)
# activate 스크립트는 추적, node 바이너리/전역설치만 제외
/.toolchain/node
/.toolchain/npm-global

# remotion render cache
/.remotion

# runtime job data (generated MP4s, projects, status, scratch files)
/data/*
!/data/.gitkeep

# oh-my-claudecode local state
/.omc

# misc
.DS_Store
*.pem
.env*.local

# logs
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# skills CLI install target (we keep a curated copy in src/content)
/.agents
.gstack/
/eval/results/
